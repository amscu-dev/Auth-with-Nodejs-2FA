/#path
  #pathMethod
   #tags
   #summary
   #description
   #parameters
   - (name of queryParam) -- exemplul de mai jos descrie un query param
      # description
      # in(trebuie precizat unde este acest parametru): path, query, header, cookie (acestea sunt valorile permise, acum luam cazul 'query')  
      # schema:
        #type : integer/object/array
        #minimum
        #maximum - parametrii pentru integerul nostru
      # required: true/false   
      # example: exemplu
   - (name of path param) -- exemplul de mai jos descrie un path param ( trebuiue sa pui la path /path/{pathParam})
     # description
     # in: path
     # required: true (aici este obligatoriu sa punem required true)
     # schema:
       # type: string
     # example/examples(poti da mai multe examples): exemplu
   #responses
    #cod
      #description
      #content
        #contenti-type
          #schema
            (optional #oneof)
            #type: object
            #properties:
              name:
                #type:
                #example
            #type: array
            #items:
              #type: string
              #example: exemple
   #requestBody
    #required
    #content:
     #content-type
      #schema
       #type object
       #properties
         #propname
            #type
            #description
            #minLenght
            #maxLength
            #format
            #pattern
            #example
            #minimum(pt integer type)
            #maximum(pt integer type)
            #enum
             - valoare enum 1
             - valoare enum 2
             - valoare enum 3
            #minItems(pt array type)
            #maxItems(pt array type)


--------------------------------------------------------
        #responses
          #schema
          #examples: poti pune multiple exemple aici si alege (dar trebuie sa ,i respecte schema gen)
            #nume-exemplu-01
              #summary 
              #value
                - prop 1
                  prop 2
                - prop 1
                  prop 2
            #nume-exemplu-02
              #summary 
              #value
                - prop 1
                  prop 2
                